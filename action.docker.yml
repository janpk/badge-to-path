name: 'Badge To Path'
description: 'Description of my action'
inputs:
  github-token:
    description: 'GitHub token'
    required: false
runs:
  using: 'docker'
  image: 'docker://ghcr.io/janpk/badge-to-path.git:main'
